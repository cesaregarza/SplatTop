{{- if .Values.celeryBeat.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "splattop.fullname" . }}-celery-beat
  labels:
    {{- include "splattop.labels" . | nindent 4 }}
    {{- include "splattop.celeryBeat.matchLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.celeryBeat.replicaCount }}
  selector:
    matchLabels:
      {{- include "splattop.celeryBeat.matchLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "splattop.celeryBeat.matchLabels" . | nindent 8 }}
    spec:
      {{- if .Values.celeryBeat.imagePullSecrets }}
      imagePullSecrets:
        {{- range .Values.celeryBeat.imagePullSecrets }}
        - name: {{ .name | default . }}
        {{- end }}
      {{- end }}
      containers:
        - name: celery-beat
          image: "{{ .Values.celeryBeat.image.repository }}:{{ .Values.celeryBeat.image.tag }}"
          imagePullPolicy: {{ .Values.celeryBeat.image.pullPolicy }}
          {{- with .Values.celeryBeat.command }}
          command:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.celeryBeat.args }}
          args:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            {{- range $name, $value := .Values.celeryBeat.env }}
            - name: {{ $name }}
              value: {{ $value | quote }}
            {{- end }}
            {{- $secret := .Values.celeryBeat.secretRefs }}
            {{- if and $secret $secret.name $secret.keys }}
            {{- range $key := $secret.keys }}
            - name: {{ $key }}
              valueFrom:
                secretKeyRef:
                  name: {{ $secret.name }}
                  key: {{ $key }}
            {{- end }}
            {{- end }}
          {{- with .Values.celeryBeat.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.celeryBeat.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.celeryBeat.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.celeryBeat.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
