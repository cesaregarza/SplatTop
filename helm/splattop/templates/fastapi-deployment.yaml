{{- if .Values.fastApi.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "splattop.fullname" . }}-fast-api
  labels:
    {{- include "splattop.labels" . | nindent 4 }}
    {{- include "splattop.fastapi.matchLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.fastApi.replicaCount }}
  selector:
    matchLabels:
      {{- include "splattop.fastapi.matchLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "splattop.fastapi.matchLabels" . | nindent 8 }}
    spec:
      {{- if .Values.fastApi.imagePullSecrets }}
      imagePullSecrets:
        {{- range .Values.fastApi.imagePullSecrets }}
        - name: {{ .name | default . }}
        {{- end }}
      {{- end }}
      containers:
        - name: fast-api-app
          image: "{{ .Values.fastApi.image.repository }}:{{ .Values.fastApi.image.tag }}"
          imagePullPolicy: {{ .Values.fastApi.image.pullPolicy }}
          ports:
            {{- range .Values.fastApi.containerPorts }}
            - name: {{ .name }}
              containerPort: {{ .containerPort }}
            {{- end }}
          env:
            {{- range $name, $value := .Values.fastApi.env }}
            - name: {{ $name }}
              value: {{ $value | quote }}
            {{- end }}
            {{- $secret := .Values.fastApi.secretRefs }}
            {{- if and $secret $secret.name $secret.keys }}
            {{- range $key := $secret.keys }}
            - name: {{ $key }}
              valueFrom:
                secretKeyRef:
                  name: {{ $secret.name }}
                  key: {{ $key }}
            {{- end }}
            {{- end }}
          {{- with .Values.fastApi.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- if .Values.fastApi.nodeSelector }}
      nodeSelector:
        {{- toYaml .Values.fastApi.nodeSelector | nindent 8 }}
      {{- end }}
      {{- if .Values.fastApi.affinity }}
      affinity:
        {{- toYaml .Values.fastApi.affinity | nindent 8 }}
      {{- end }}
      {{- if .Values.fastApi.tolerations }}
      tolerations:
        {{- toYaml .Values.fastApi.tolerations | nindent 8 }}
      {{- end }}
{{- end }}
